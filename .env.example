# Environment Variables for Production

# Server Configuration
PORT=3002
NODE_ENV=production

# Database - Choose one:
# Option 1: MongoDB Atlas (Recommended for production)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/Lapit?retryWrites=true&w=majority

# Option 2: Local MongoDB
# MONGODB_URI=mongodb://localhost:27017/Lapit

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# AWS Configuration
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1

# S3 Bucket Configuration
S3_BUCKET_NAME=mern-github-storage

# Rate Limiting (Optional - adjust as needed)
# These are set in middleware/rateLimiter.js but can be overridden
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100
